<UserControl x:Class="ColorTrainer.Views.CustomMessageBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="350"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Tag="{Binding RelativeSource={RelativeSource Self}}">
    <Border Padding="20" Background="{DynamicResource SecondaryBackgroundColor}" CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Заголовок -->
                <RowDefinition Height="*"/>
                <!-- Сообщение -->
                <RowDefinition Height="Auto"/>
                <!-- Кнопка -->
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Grid.Row="0" Text="Заголовок" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextColor}" Margin="0,0,0,10"/>

            <TextBlock x:Name="MessageText" Grid.Row="1" Text="Текст сообщения здесь..." FontSize="16" Foreground="{DynamicResource PrimaryTextColor}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="0,10"/>

            <Button x:Name="OkButton" Grid.Row="2" Content="ОК" Style="{StaticResource MenuButton}" HorizontalAlignment="Right" Width="120" Height="40"/>
        </Grid>
    </Border>
</UserControl>